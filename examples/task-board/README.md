# Task Board Example

This example is a small task board with add, complete, and filter interactions.

Run it with:

```bash
qazy user-scenarios/task-flow
```

Or use prompt mode:

```bash
qazy -p "add a task, complete it, and verify the completed filter" --no-use-cookie
```
