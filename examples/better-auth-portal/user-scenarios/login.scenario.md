---
start_page: /dashboard
use_cookie: true
auth_provider: better-auth
---

# Better Auth Login

## Notes

Qazy signs in through the Better Auth built-in flow before handing control to the
runtime. Credentials come from `scenarioDefaults` in `qazy.config.json`.

## List

- [ ] Confirm the dashboard loads with the "Welcome back, Student" header.
- [ ] Confirm all three assignment cards are visible (Physics Lab, History Essay, Calculus Problem Set).
- [ ] Confirm the header shows `student@example.com`.
