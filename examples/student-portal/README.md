# Student Portal Example

This example is a small student login and dashboard flow with a profile menu and assignment cards.

Run it with:

```bash
qazy user-scenarios/login
```

Or use prompt mode:

```bash
qazy -p "sign in as the student, confirm the dashboard loads, and open the profile menu" --no-use-cookie
```
